Docker Compose-1.14.0
===

armhf:

```
sudo wget -O /usr/local/bin/docker-compose https://github.com/vimagick/rpi-bin/raw/master/bin/docker-compose-1.14.0-armv7h &&
sudo chmod +x /usr/local/bin/docker-compose
```

amd64:

```
sudo wget -O /usr/local/bin/docker-compose https://github.com/docker/compose/releases/download/1.14.0/docker-compose-Linux-x86_64 &&
sudo chmod +x /usr/local/bin/docker-compose
```
